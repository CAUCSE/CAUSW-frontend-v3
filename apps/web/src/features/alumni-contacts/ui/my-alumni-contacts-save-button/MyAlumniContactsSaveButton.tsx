import { ActionHeader } from '@/shared/ui';

export const MyAlumniContactsSaveButton = () => {
  return (
    <ActionHeader.ActionButton type="submit">
      저장하기
    </ActionHeader.ActionButton>
  );
};
